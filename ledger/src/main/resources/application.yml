spring:
  datasource:
    url: jdbc:mariadb://192.168.57.125:18887/ledger
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: mariadb
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create