spring:
  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/ledger
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: mariadb
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create